# Upstream Sync — 2026-02-06

## Summary

First upstream sync for the randomm/opencode fork. Merged 47 upstream commits from anomalyco/dev (0d22068c9..95ad6758a) into sync/upstream-2026-02-06 branch. 10 merge conflicts resolved — 4 easy (accept theirs/ours pattern), 1 manual (copilot.ts anthropic-beta header), 5 auto-resolved by git. All 3 fork features survived intact. Feature verification: 30/30 tests pass. No absorption signals detected.

## Upstream Changes

Commit range merged: `0d22068c9..95ad6758a` (47 commits)

| Area        | Commits | Summary                                                                                                                                                  |
| ----------- | ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| provider    | 2       | Trinity model system prompt; fix Amazon Bedrock prefix selection                                                                                         |
| tui         | 4       | Restore footer; highlight esc on hover; improve skills dialog; parallelize skill downloads                                                               |
| desktop     | 7       | macOS editor display; loading window + Rust restructure; server URL logic; titlebar maximize; key accelerators; menu bar                                 |
| app         | 6       | Task tool render fix; session last-updated in command palette; retry error unwrap; mobile a11y; error boundary; narrow viewport                          |
| config      | 1       | Handle $ character with {file:} pattern                                                                                                                  |
| infra       | 10      | 4× chore:generate; 2× nix hashes; Windows test runner; GitLab AI bump; deps update; rm hardcoded conditional                                             |
| tool/plugin | 3       | Directory parameter for multi-project plugin; load user plugins after builtin; fix abort with queued messages                                            |
| docs/web    | 3       | i18n for web; locale-specific URLs; fix z icon color                                                                                                     |
| other       | 11      | Refactoring/tests split (×2); fix /data API share URLs; skill discovery from URLs; fix usage graph; senior engineer prompt; dax silence; release v1.1.53 |

## Conflicts Resolved

| File                                      | Strategy      | Details                                                                                                           |
| ----------------------------------------- | ------------- | ----------------------------------------------------------------------------------------------------------------- |
| `bun.lock`                                | Accept theirs | Standard lockfile resolution                                                                                      |
| `AGENTS.md`                               | Keep ours     | Fork-specific governance document                                                                                 |
| `README.md`                               | Accept theirs | Upstream documentation                                                                                            |
| `.github/workflows/test.yml`              | Keep deletion | Our fork removed upstream CI, uses own ci.yml                                                                     |
| `packages/opencode/src/plugin/copilot.ts` | Manual merge  | Accept upstream's `input`→`incoming` rename; keep our extended anthropic-beta header with `context-1m-2025-08-07` |
| `packages/opencode/src/session/prompt.ts` | Auto-resolved | Upstream removed callback rejection loops; our fork had modified them. Accepted upstream's removal.               |
| `packages/opencode/src/bun/index.ts`      | Auto-resolved | Both sides modified; no marker conflicts                                                                          |
| `packages/opencode/src/flag/flag.ts`      | Auto-resolved | Both sides modified; no marker conflicts                                                                          |
| `packages/opencode/src/skill/skill.ts`    | Auto-resolved | Both sides modified; no marker conflicts                                                                          |
| `packages/opencode/src/tool/task.ts`      | Auto-resolved | Our complete replacement (async background tasks) kept; upstream's refactoring of their version did not conflict  |

## Feature Verification

- ✅ **async-tasks**: 30/30 checks pass — all newFiles exist, criticalCode markers present, test files exist
- ✅ **rg-tool**: all checks pass — newFiles exist, deletedFiles stay gone, criticalCode present
- ✅ **1m-context**: all checks pass — modifiedFiles contain criticalCode markers

## Absorption Alerts

No absorption signals detected for any feature:

- `backgroundTaskResults`, `BackgroundTask`, `task.*concurrency.*slot` — NOT in upstream
- `RgTool`, `ripgrep.*tool`, `rg --json` — NOT in upstream
- `context-1m-2025-08-07`, `context.limit.*1000000` — NOT in upstream

## Decisions Made

1. **copilot.ts anthropic-beta header**: Keep our extended header with context-1m while accepting upstream's variable rename
2. **prompt.ts callback rejection**: Accept upstream's removal of callback rejection loops (abort signal handles cleanup)
3. **task.ts**: Keep our complete async-tasks replacement (upstream's session_id→task_id refactor does not apply to our version)
4. **test.yml**: Keep our deletion (we have our own CI)

## Pain Points

1. **Developer agent timeout**: The initial all-in-one merge task timed out (>120s). Breaking into smaller focused tasks worked better.
2. **bun test segfault**: Test suite crashed with segfault, preventing full pass/fail count. Pre-existing issue.
3. **Missing external deps**: 6 typecheck errors from missing `@gitlab/opencode-gitlab-auth`, `ai-gateway-provider`, and `@agentclientprotocol/sdk` — these are private/enterprise packages not in our environment.

## Pre-existing Issues (Not Merge-Related)

- **Test failures**: Null-byte-in-path ENOENT bug causes ~47 test failures across permission, snapshot, skill, and config tests
- **Typecheck errors**: 6 errors from missing external modules (gitlab auth, AI gateway provider, ACP SDK)
- **Bun segfault**: Test runner crashes intermittently

## Recommendations for Next Sync

1. **Watch upstream task tool**: Upstream renamed `session_id`→`task_id` and removed Bus subscription tracking. If they continue evolving the task tool, our async-tasks divergence will grow. Consider contributing async-tasks upstream (Issue #163).
2. **Monitor ai-gateway-provider**: Upstream now depends on `ai-gateway-provider` package for Cloudflare. Ensure `bun install` resolves it.
3. **Address null-byte path bug**: The ENOENT test failures mask real issues. Fix before next sync to get clean test baselines.
4. **prompt.ts callback removal**: Verify no edge cases where sessions hang after abort without callback rejection. The upstream change looks correct but warrants testing.
5. **Break sync tasks into <2min chunks**: Single monolithic merge tasks time out. Use focused sub-tasks.
