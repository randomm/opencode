# Sync Report: 2026-04-03

This report documents the sync operation performed on April 3, 2026.

## Post-Sync Resolution (2026-04-04)

### Summary

The initial merge attempt committed conflict markers undetected. Required 3 reset-and-retry cycles before achieving a clean merge. Typecheck initially showed 400+ errors from upstream API changes; all resolved by updating fork files to new upstream APIs.

### API Drift Fixed

| Old API | New API | Affected Fork Files |
|---------|---------|---------------------|
| `formatElapsed` | `formatDuration` | `tasks/job-commands.ts` |
| `cleanupSessionTaskMaps` | removed | `session/async-tasks.ts` |
| `Session.createNext` | internal-only | `tasks/*`, replaced with `createSessionInDirectory()` helper |
| `context` from `project/instance.ts` | not exported | `tasks/pulse*.ts` |
| `OPENCODE_DISABLE_GLOBAL_SKILLS` | `OPENCODE_DISABLE_EXTERNAL_SKILLS` | `skill/skill.ts` |
| Raw string IDs | Branded types requiring `.make()` | `tasks/*`, `session/async-tasks.ts`, `test/*` |
| `Worktree.create rootPath` param | removed | `tasks/pulse-scheduler.ts` |

### bash.ts Upstream Rewrite

Upstream replaced `bash.ts` (~400 line refactor):
- Old: `spawn()` + `realpathSync.native` workdir validation
- New: Effect/ChildProcess spawner + cross-platform `resolvePath()`
- **Decision (Option A)**: Ported our workdir validation on top of upstream's new `resolvePath()`. Both Existence check (`Filesystem.isDir`) and boundary check (`Instance.containsPath`) preserved. `realpathSync.native` removed — upstream's `resolvePath` now handles symlinks cross-platform.

### Manifest Updates

- `bash-workdir-validation`: Updated criticalCode, added driftNotes, updated absorptionSignals
- `async-tasks`: Removed stale markers, added `reserveTaskSlot`, `getSessionTaskCount`, `MAX_STORED_TASK_RESULTS`
- `taskctl`: Updated criticalCode to actual code strings (not human-readable descriptions), added new source files

### Final Gate Results

- `bun test .fork-features/verify.ts`: **240 pass, 0 fail**
- `bun run typecheck`: **0 errors**
- `bun run build`: **✅ binary produced** (opencode-darwin-arm64, 120MB)
- `bun dev --help`: **✅ smoke test passing**
- Fork-specific tests: **all passing** (async-tasks, rg, bash-workdir, store, cerebras, task, permission)

### Recommendations for Next Sync

1. **Pre-commit hook for conflict markers** — run `git grep '<<<<<<< HEAD'` before any merge commit to catch this early
2. **Sweep pattern** — always sweep `src/` and `test/` to upstream first, then restore fork files, then verify markers
3. **Expect API drift** — upstream is actively effectifying all services; assume any effectified file may rename or remove exports our fork files depend on
4. **Manifest criticalCode must be literal code strings** — not human-readable descriptions; verify.ts will catch non-literal markers